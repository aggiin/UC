# 嵌入算法 Python 实现 - 中文说明

## 任务完成总结

本次任务已经成功完成：从 C++ 代码 `src/uc/2way/embedding.cpp` 中提取嵌入生成代码，并将其转换为独立的 Python 代码。

## 创建的文件

### 1. embedding_algorithm.py (27KB)
这是核心实现文件，包含完整的嵌入算法 Python 实现。

**主要内容：**
- **数据结构类：**
  - `Node`: Valiant DAG 中的基本节点
  - `Gamma1Node`: Gamma1 图中的节点（最多一条入边和出边）
  - `Gamma2Node`: Gamma2 图中的节点（最多两条入边和出边）
  - `ValiantDAG`: 主要的 DAG 结构类

- **核心函数：**
  - `node2()`, `node3()`, `node4()`: 处理 2、3、4 个节点的基本情况嵌入
  - `pathfinder()`: 在通用图中查找路径的递归算法
  - `set_indices()`: 根据输入输出数量设置索引
  - `check_side()`: 检查嵌入边的方向（左或右）
  - `embedding_with_supergraph()`: 基于超图确定嵌入策略
  - `embed_side()`: 对左侧或右侧进行嵌入
  - `embedding_merged()`: 混合构造的主嵌入函数
  - `neighbouring_index()`: 获取相邻索引的辅助函数

### 2. EMBEDDING_README.md (8KB)
详细的双语（中英文）文档。

**文档内容：**
- 核心概念解释（通用电路、嵌入算法）
- 主要数据结构详解
- 每个函数的详细说明，包括参数、返回值、工作原理
- 算法复杂度分析（时间 O(n log n)，空间 O(n)）
- 使用示例代码
- 技术细节（极点索引、节点数组、控制位、递归结构）
- 参考文献

### 3. example_usage.py (7KB)
可运行的示例脚本，演示如何使用嵌入算法。

**包含 5 个示例：**
1. 创建基本节点结构
2. 创建 Valiant DAG 结构
3. 使用 neighbouring_index 函数
4. 演示基本情况嵌入（2、3、4 个极点）
5. 算法工作原理的详细解释

**运行方法：**
```bash
python3 example_usage.py
```

### 4. .gitignore
防止 Python 缓存文件和构建产物被提交到版本控制。

## 快速开始

### 1. 查看算法说明
```bash
# 查看主要文档
cat EMBEDDING_README.md

# 或者直接运行主模块查看简介
python3 embedding_algorithm.py
```

### 2. 运行示例
```bash
# 运行交互式示例
python3 example_usage.py
```

### 3. 在代码中使用
```python
from embedding_algorithm import ValiantDAG, Node, embedding_merged

# 创建 Valiant DAG
dag = ValiantDAG(pole_number=8)

# 使用各种函数...
```

## 算法说明

### 什么是嵌入算法？

嵌入算法是 Valiant 通用电路构造的核心部分。它的作用是将原始电路的边映射到通用图结构上。通用图是一个可以通过编程来模拟任意给定大小电路的结构。

### 工作原理

1. **基本情况**：对于 2、3、4 个节点的小图，直接进行嵌入
2. **递归分解**：对于更大的图，递归地分解为左右子图
3. **路径查找**：使用 pathfinder 函数在图中找到连接两个极点的路径
4. **控制位设置**：设置开关节点的控制位来实现所需的连接

### 关键概念

- **极点（Poles）**：图的输入和输出点
- **节点数组（Node Array）**：用于路由的中间节点
- **左右子图（Left/Right Subgraphs）**：递归结构的两个分支
- **控制位（Control Bits）**：用于编程通用电路的位

### 索引规则

- 偶数索引 2k → 进入左子图的第 k 个极点
- 奇数索引 2k+1 → 进入右子图的第 k 个极点
- 每对极点有 3 个中间节点用于路由

## 与原始 C++ 代码的对应关系

| C++ 文件 | Python 对应 | 说明 |
|---------|------------|------|
| `embedding.cpp` 中的 `node2()` | `ValiantDAG.node2()` | 2 节点基本情况 |
| `embedding.cpp` 中的 `node3()` | `ValiantDAG.node3()` | 3 节点基本情况 |
| `embedding.cpp` 中的 `node4()` | `ValiantDAG.node4()` | 4 节点基本情况 |
| `embedding.cpp` 中的 `pathfinder()` | `ValiantDAG.pathfinder()` | 路径查找算法 |
| `embedding.cpp` 中的 `embedding_with_supergraph()` | `embedding_with_supergraph()` | 超图嵌入 |
| `embedding.cpp` 中的 `embed_side()` | `embed_side()` | 侧面嵌入 |
| `embedding.cpp` 中的 `embedding_merged()` | `embedding_merged()` | 合并嵌入 |

## 代码质量保证

✅ 所有代码都已通过 Python 语法检查  
✅ 示例脚本可以成功运行  
✅ 包含完整的类型提示  
✅ 添加了详细的文档字符串  
✅ 代码结构清晰，易于理解和维护  

## 参考资料

1. **原始论文：**
   - Leslie G. Valiant, "Universal circuits (Preliminary Report)", STOC 1976
   - Ágnes Kiss and Thomas Schneider, "Valiant's Universal Circuit is Practical", Eurocrypt 2016

2. **原始实现：**
   - GitHub: https://github.com/encryptogroup/UC
   - 文件: `src/uc/2way/embedding.cpp`

3. **相关论文：**
   - Daniel Günther, Ágnes Kiss and Thomas Schneider, "More Efficient Universal Circuit Constructions", Asiacrypt 2017

## 许可证

GNU Affero General Public License v3.0

原始 C++ 实现版权所有 (C) 2016 Engineering Cryptographic Protocols Group, TU Darmstadt

## 常见问题

**Q: 这个 Python 实现可以直接用于生产环境吗？**  
A: 这个实现主要用于教育和研究目的。如需在生产环境使用，建议使用原始的 C++ 实现，因为它经过了更严格的测试和优化。

**Q: 如何验证嵌入的正确性？**  
A: 原始 C++ 实现有 `DEBUG_CORRECTNESS` 标志来验证正确性。Python 版本可以通过对比结果来验证。

**Q: 这个实现的性能如何？**  
A: 算法复杂度是 O(n log n)，其中 n 是电路大小。Python 版本会比 C++ 版本慢，但算法本身是相同的。

**Q: 可以用这个代码做什么？**  
A: 
- 学习 Valiant 通用电路的嵌入算法
- 研究通用电路的构造方法
- 作为其他通用电路实现的参考
- 教学演示

## 联系与支持

如有问题，可以：
1. 查看原始项目：https://github.com/encryptogroup/UC
2. 阅读相关论文了解算法细节
3. 查看示例代码 `example_usage.py` 了解使用方法

---

*此 Python 实现由 C++ 代码翻译而来，保留了原始算法的所有核心逻辑。*
